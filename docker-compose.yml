version: '3.8'

services:

  # PostgreSQL → Supabase로 이전. 로컬 컨테이너 불필요.
  # postgres:
  #   image: postgres:15
  #   ...

# 볼륨 - postgres_data 제거 (Supabase 사용)
volumes: {}

# ==============================
# 네트워크 - 컨테이너끼리 이름으로 통신
# ==============================
networks:
  hindsight-net:
    driver: bridge
