# 주가 데이터 수집 (Stooq - 무료, API 키 불필요, 전체 기간 제공)
requests==2.32.3

# 거시지표 수집 (FRED API)
fredapi==0.5.2

# AI 뉴스 요약 (Gemini API)
google-generativeai==0.8.5

# PostgreSQL 드라이버 (psycopg v3 - SCRAM-SHA-256 및 인코딩 안정성)
psycopg[binary]==3.2.6

# Elasticsearch 클라이언트
elasticsearch==8.17.2

# 환경변수 로딩 (.env 파일)
python-dotenv==1.1.0

# 데이터 처리
pandas==2.2.3

# 기술적 지표 계산 (RSI, MACD, 일목균형표)
pandas-ta==0.4.71b0

# Kafka 연동
kafka-python==2.0.6

# BigQuery 클라이언트 (GDELT 뉴스 수집)
google-cloud-bigquery==3.27.0
db-dtypes==1.3.1
